a(href="{{pathFor 'eatseryPage'}}")
	.card
		.card-image-preview(style="background-image:url(#{imagePath})")
		.card-details
			p.card-title #{name}
			p.card-distance #{distanceText}
			p.card-address #{address}